# ============================================================
# CCTV SMART MONITOR - REQUIRED PACKAGES
# ============================================================
# Run: pip install -r requirements.txt
#
# NOTE: dlib/face_recognition is OPTIONAL (see below)
# Everything else works without it!
# ============================================================

# Core (REQUIRED - always install)
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Number Plate & Text Recognition
easyocr>=1.7.0
imutils>=0.5.4

# Object/Vehicle Detection (YOLO)
ultralytics>=8.0.0

# Web Dashboard
flask>=3.0.0
flask-socketio>=5.3.0
flask-login>=0.6.0

# Alerts
requests>=2.31.0

# Image Processing & Night Mode
scikit-image>=0.21.0

# Scheduling & Reports
schedule>=1.2.0
fpdf2>=2.7.0

# Configuration
pyyaml>=6.0.0

# Desktop GUI Application
customtkinter>=5.2.0

# Utilities
python-dateutil>=2.8.0
colorama>=0.4.6
tqdm>=4.65.0

# ============================================================
# OPTIONAL PACKAGES (install separately if needed)
# ============================================================
# These require extra setup on Windows.
# The system works WITHOUT them (uses OpenCV fallback).
#
# --- Face Recognition (needs Visual Studio C++ Build Tools) ---
# pip install dlib face-recognition
#
# --- Telegram Bot ---
# pip install python-telegram-bot
#
# --- WhatsApp via Twilio ---
# pip install twilio
#
# --- Matplotlib (for charts in reports) ---
# pip install matplotlib
# ============================================================
